{ ... }:

{
  plugins = {
    cmp = {
      enable = true;
      autoEnableSources = true;
      settings = {
        completion.autoComplete = true;
      };
    };
    cmp-latex-symbols.enable = true;
  };
}
