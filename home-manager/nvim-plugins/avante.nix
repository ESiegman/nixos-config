{ ... }:

{
  plugins.avante = {
    enable = true;
    settings = {
      provider = "copilot";
      mappings = {
        ask = "<leader>aa";
        edit = "<leader>ae";
        refresh = "<leaderar>";
      };
    };
  };
}
